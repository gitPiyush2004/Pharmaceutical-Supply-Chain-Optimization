# ---------------------------------------------------------------------------
# PharmaChain Analytics - runtime dependencies
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Core data stack
# numpy is pinned for the same reason as the ML libraries below - the pickled
# models carry numpy arrays, and the array format is version-sensitive.
pandas>=2.0.0
numpy==2.4.4
PyYAML>=6.0

# Machine learning
# Pinned, unlike the rest of this file, because models/*.joblib are pickles:
# they are written by a specific version of these libraries and read back by
# whatever version the environment happens to resolve. A floating upper bound
# means a deployment can install a newer scikit-learn and fail to unpickle a
# model that worked yesterday, on a build that otherwise looks healthy. These
# are the versions the committed models were trained with.
scikit-learn==1.9.0
xgboost==3.3.0
joblib>=1.3.0

# Statistics / experimentation
scipy>=1.10.0
statsmodels>=0.14.0

# Visualisation
plotly>=5.20.0
matplotlib>=3.7.0

# Application layer
streamlit>=1.32.0

# I/O
openpyxl>=3.1.0

# Testing (development)
pytest>=7.4.0
